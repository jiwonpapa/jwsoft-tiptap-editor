# 기여 가이드

기여한 코드는 별도 명시가 없는 한 프로젝트의 Apache-2.0 라이선스 조건으로 제공됩니다. 제3자 코드를 포함할 때 원래 라이선스와 저작권 고지를 보존하십시오.

1. `CONSTITUTION.md`와 `AGENTS.md`를 먼저 읽습니다.
2. 변경 전에 관련 ADR과 동등성 항목을 확인합니다.
3. 한 PR은 한 목적만 다룹니다.
4. 공개 동작 변경은 문서, 테스트, CHANGELOG를 함께 수정합니다.
5. `make check`와 변경 범위의 통합 테스트를 통과해야 합니다.
6. 보안 문제는 공개 이슈로 재현 payload를 올리지 말고 `SECURITY.md`를 따릅니다.
7. [언어·코딩 규약](docs/13-engineering-standards.md)을 따릅니다. 주 하네스는 Python, 단순 배포는 Shell입니다.
8. `make audit` 및 변경 범위의 `make browser-check`를 실행합니다. 부정 테스트 없이 차단 규칙을 완료 처리하지 않습니다.
9. 검사 해제·기술 부채 상한 증가/만료 연장·커버리지 하향은 일반 수정으로 숨기지 않고 승인과 근거를 남깁니다.

브랜치는 `codex/`, `feat/`, `fix/`, `docs/` 접두사를 사용합니다. 커밋은 무엇을 왜 변경했는지 설명합니다.
